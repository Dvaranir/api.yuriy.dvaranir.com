'use strict';

/**
 * schedule-animation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::schedule-animation.schedule-animation');
