'use strict';

/**
 * schedule-animation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::schedule-animation.schedule-animation');
